[package]
name = "runtime-host"
version = "0.1.0"
edition = "2021"

[dependencies]
core-api = { path = "../core-api" }
core-security = { path = "../core-security" }
windows-compat = { path = "../windows-compat" }
serde = { version = "1.0", features = ["derive"] }
